■スクリプト命令
　サンプル兼マニュアル（仮）
今作使用できる
基本的なタグについて説明します。
このファイル
の中身を見ながら読み進めてください。
■書式について
タグの詳細等は
「システムマニュアル」をご覧ください。
確認方法は「Artemis起動中」に、
Mキー押下で開けます。
Artemisが起動していないと、
正しく閲覧できないので注意してください
以降は、タグの説明になります。
■背景とイベントCGについて
背景とイベントCGタグの扱いはほぼ同じなので、
ここで合わせて解説します。
背景の切り替えは「背景名」を
記述したタグを使用します。
フェードタイムを直接指定することも可能です
時間帯のステータスは省略可能ですが、
記入しておくことを推奨します。
素材は
に格納されています。
「スプレッドシート」に
タグ名の資料があります。
拡大背景を使うときは
背景名に「大」をつけてください。
また、同一背景を単純に拡大縮小する場合は、
ノーマルフェード（無指定）だと
少しフェードが長いかと思いますので、
「拡縮」というステータスをつけることで、
time=500でフェード出来ます。
必要に応じて座標は
手動で指定する必要があります。
「zoom」を指定すれば、
更に拡大縮小が出来ます。
zoom、120を指定。
zoom、80を指定。
同一背景を連続で表示する場合は、
座標位置がリセットされないので
注意してください。
x座標、zoom値をリセット
（真ん中に戻す）
通常サイズ背景の場合、
内部的には別背景として
処理されるので、
座標などはリセットされます
大背景→小背景
こんな具合です。
今作で白と黒背景を使う場合は
「白」「黒」で指定してください。
黒
背景のスクロールの書式は、
「scroll」と「speed」を使用します。
「先に指定した座標から」
「後の指定した座標」
へスクロールしていく感じです。
基本的には後述する、
画像の端から端までスクロールする
命令が便利ですが、
特定の物を見回しているシーン等では、
座標の数値を指定してあげてください。
縦向き。
座標を指定してスクロールをおこなう際は、
数値次第で背景画像の
外まで移動してしまうので、
注意してください
背景画像の
左端から、右端へスクロールさせる命令です。
背景画像下端から、上端へスクロール。
はみ出すことはないはずですが、
もし端から端へのscrollで、
画像外まで読み込んでしまう場合は、
すみませんが連絡をお願い致します。
スクロール終了に専用命令はありません。
画像を再度読み込むことで停止できます。
大背景に戻す場合は、前述したとおり、
座標がリセットされず、scroll終了位置
を表示するため注意してください。
トランジションルールについて説明します。
場面転換を示すトランジションルールです。
建物の外観から、建物の内部等、そこまで距離が
離れていない場所に移動する際に使用します。
時間経過。
基本的には、同一背景で
時間が経過した際に使用します。
場面転換速。
急いでる場合などで使用します。
あまり多用することはないかと思われます。
ルール画像は
に格納されており、
手動で設定する事も可能です。
主人公が目を閉じた
イメージのルール画像。
目開け。
後述しますが、「bt,et」タグにも指定できます。
■メッセージウィンドウ（mw）について
メッセージウィンドウをオフにするには
「msgoff」タグを使用します。
次のクリックで
mwをオフにしたまま2秒待機します
待機終了。
背景表示等、一部のタグは自動で
メッセージウィンドウを非表示にしてくれます。
しかし、後述する「bt,et」を使った場合は
自動で非表示にならないので、必ず手動で
「msgoff」タグを使ってください。
こういうときなど。
■立ち絵について
立ち絵の表示に関して解説します。
今回使用するシステムでは、
立ち絵を入力するためのツールを
システムエンジニアさんが用意してくれています。
TABキー→システム→デバッグサーバ
を選択すると、ブラウザが開くと思われます。
もしくは、
Artemis.exe実行中に、
Tキーを押下することでも開けます
このテキストフォルダ内に、
コメントアウトで
簡易的な使用方法を記載しています。
使い方に不明な点がある場合は、
すみませんがご連絡いただけますと幸いです。
１点注意なのですが、デバッグサーバは
Artemis.exeと連携して動いているため、
ゲームを起動した状態でないと
正常に機能しません。
立ち絵は「シナリオ内で」
一度表示すると、
服装等のステータスを、
一部記録したで表示されます。
シナリオ内初登場時や、服装やポーズが
変わる際は、必ず指定を行ってください。
表情だけ変える時
ポーズと腕差分と表情を変える時
表示されている立ち絵全てを消す
立ち絵の指定に「顔」
をつけると、MWの顔絵のみを
表示することが可能です
綾希「こらー！　そこの不審者ー！」
mwに表示される、
顔絵のみを表示する場合も、
制限は立ち絵を出す場合と同じです
■立ち絵サイズについて
今作は立ち絵のサイズが4サイズあります。
奥サイズ。
前サイズ
大背景+手前立ち絵
大背景zoom120+目前サイズ立ち絵
立ち絵4サイズに対して、
背景は2サイズなので、
注意してください。
■bt,etタグについて
複数のオブジェクトを纏めて表示するときは、
「bt,et」タグを使ってください。
まとめて表示したい画像を囲むように書きます。
また、背景タグのように
自動でメッセージウィンドウが
非表示になったり、
フェード時間が自動で決定されないため、
必ず手動で書くようにしてください。
キャラと背景を同時に拡大したい場合は
こんな感じで。
場面転換などのルール画像も適用できます。
ただし、フェード時間までは
自動で決まらないので、
場面転換速を使うときは注意してください。
場面転換速以外は、
無指定で大丈夫です。
■立ち絵演出機能
複数人立ち絵を並べる際は、
立ち位置の指定が必要になります。
無記入は「中」扱いです。
今作は立ち絵が一人のみ？
のはずなので、
意図的に立ち位置を変えたい時に
使用するかと思います。
左
右
直接座標を指定することも可能です
もちろんy座標もいじれます
内部的にはもっと複数立ち位置を
指定することが可能です。
おそらく多用しないと
思われるので、
説明は省略しますが、
明示的にどこかの位置を指定する事が多く、
座標指定をする頻度が多いようであれば、
ご相談いただけますと幸いです。
■delay（音声同期）（ボイス未着につき未対応）
綾希「ふふっ。あんまりにも熱中してそうだったから、普通に声かけても聞こえないと思って」
ディレイ記述です。
sliファイルの制作忘れ、
コミット忘れにご注意ください。
ボイスのあるテキストで、
「Wキー」を押下することで
ループチューナーが起動します。
アクション、SE、
表情変更、出・消系が指定できます。
綾希「ふふっ。あんまりにも熱中してそうだったから、普通に声かけても聞こえないと思って」
顔絵のみの表示でもdelayは入れられます。
■立ち絵その他
立ち絵は動きをともなった表示、
非表示方法があります。
右にスライドさせながら表示しました。
上・下・左・右あります。
消すことも出来ます。
１点注意なのですが、
アクションとスライドインを同時に使用すると、
スライドインが正常に動かないので
注意してください。
立ち絵アクション。
以下、サンプル。
びっくり
ジャンプ
あくび
おじぎ
うなづく
うんうん
縦揺れ
いいえ
ドッキリ
ゆらゆら
du/ud/lr/rlでそれぞれアクションが付きます。
クエイクで、タグと同じ
パラメータを立ち絵に対して
かけられるようになりました。
ただし無限再生時にクイックジャンプで
停止してしまうのはと
変わらないので気をつけてください。
停止。
補足ですが、アクションが動いている間に
他のアクションの指定が入ると、
上手く動かない事があるので注意してください。
■その他の演出機能
シネスコ
立ち絵の奥に表示する場合。
シネスコ２（２：１サイズ）
シネスコ２を立ち絵の奥に表示する場合。
シネスコ白。
透過なし。
シネスコ３
シネスコ枠。
白もあります。
クエイク系
mw揺れ。
画面のみ揺れ。
横揺れ。
縦揺れ。
集中線。
集中線消し。
フラッシュ。
回想演出
回想演出戻る。
こんな事もできたりします
戻し。
■音周り
「BGM」の再生は、
「bgmファイル名」
で行えます。
停止。
自動でフェードアウトしますが、
timeを指定することで
時間を指定することも可能です。
bgm003を再生
SEも同様です
SEを再生
※現在seがないようなので仮置き
ループ再生も可能です
idを割り振ることで、複数のSEを
同時に再生することも可能です。
超注意点として、
ループ再生したSEは、
必ず明示的に停止しないと、
バックログジャンプ時に
再度鳴り出してしまうので、
必ず個別に停止してください。
先に鳴らしているSEを停止
後から鳴らしたSEを停止。
se停止は、bgmと違って、
デフォルトの指定だと、
瞬間停止するので
注意してください。
おわり。
